enum FunctionalFieldMode {
    Functional,
    Simple,
}

enum ButtonType {
    OK,
    Cancel,
}

enum EditorLanguage {
    JS,
    JSON,
}

export { ButtonType, EditorLanguage, FunctionalFieldMode };